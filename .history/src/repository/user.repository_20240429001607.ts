import User from "../models/user.model";

export default class UserRepository { 

    constructor() { 

        try {
            
        } catch (error) {
            console.log("Error initializing a new ")
        }
    }
}